import { HeaderContainer } from "../../styles/Header/HeaderContainer";
import { MainMenu } from "../MainMenu/MainMenu";

export const Header = () => (
    <HeaderContainer>
        <h1>Meus Filmes</h1>

        <MainMenu />
    </HeaderContainer>
);